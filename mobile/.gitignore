# Dependencies
node_modules/

# Native build artifacts (regenerated by `npx cap add`)
# Keep android/ and ios/ tracked so native config is versioned.
# Exclude their build outputs:
android/.gradle/
android/app/build/
android/build/
android/capacitor-cordova-android-plugins/build/
ios/App/Pods/
ios/App/build/
ios/DerivedData/

# OS files
.DS_Store
*.swp
